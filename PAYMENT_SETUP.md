# إعداد شحن الساعات — ورّيني

تمت إضافة:
- `/plans` لثلاث باقات: 2 ساعة = 19 جنيه، 5 ساعات = 45 جنيه، 10 ساعات = 79 جنيه مع عرض والأكثر مبيعًا.
- `/reset-password` لإكمال رابط "نسيت كلمة المرور".
- `/payment/complete` لعرض نتيجة الدفع.
- Paymob Intention API + Unified Checkout.
- Webhook HMAC-SHA512 ومكافأة الساعات بشكل idempotent.
- Migration `019_hour_purchases.sql`.

## المطلوب منك

من لوحة Paymob المصرية تحتاج:
- Secret Key
- Public Key
- HMAC Secret
- Card Integration ID

وفي الاستضافة:
- `APP_URL=https://دومينك`
- `SUPABASE_SERVICE_ROLE_KEY` من Supabase (Server Only)

واحتفظ بالـ Secret Key وHMAC وService Role Key في Environment Variables فقط.

قبل الدفع الحقيقي، نفّذ migrations 018 و019 في Supabase. وبعد نشر الموقع اجعل `APP_URL` هو رابط الموقع الحقيقي.

Paymob يستخدم `POST /v1/intention/` لإنشاء عملية الدفع ثم Unified Checkout. تأكيد إضافة الساعات يعتمد على الـ webhook الموثق بـ HMAC، وليس على رجوع المتصفح.

## تحديث: الدفع اليدوي (Instapay / فودافون كاش) — لحد ما توافق Paymob

صفحة `/plans` بقت مؤقتًا مش بتستخدم Paymob (لسه موجود في الكود جاهز، بس متسيّب "سيتم تفعيلها قريبًا" في واجهة اختيار وسيلة الدفع). بدل ما المستخدم يروح لبوابة دفع، دلوقتي بيحصل الآتي:

1. المستخدم يختار الباقة، بعدين يختار Instapay أو فودافون كاش.
2. الموقع يوريه رقم/حساب الاستلام (`lib/payment-methods.ts` — **لازم تحط فيه بياناتك الحقيقية قبل النشر**).
3. المستخدم يحوّل بنفسه من تطبيق Instapay/فودافون كاش، وبيدخل رقم الموبايل اللي حوّل منه في الموقع ويضغط "أكدت التحويل".
4. الموقع بينشئ صف في `hour_purchases` بحالة `awaiting_review` مع كود مرجعي فريد زي `WR-A1B2C3` (عبر RPC `request_manual_hour_purchase`، migration `026_manual_payment_methods.sql`).

### ازاي الموقع هيعرف إن المستخدم دفع فعلاً؟

**الحقيقة المهمة:** أرقام Instapay/فودافون كاش الشخصية (مش حساب تاجر/Business) **مفيش لها API عام** يبعتلك webhook زي Paymob، فمفيش طريقة يتأكد بيها الموقع تلقائيًا إن الفلوس وصلت. الحل هو مراجعة يدوية بسيطة، وده بالظبط نفس الأسلوب المستخدم بالفعل في المشروع لطلبات السحب (`payout_requests` / `process_payout_request`):

1. تفتح تطبيق Instapay أو فودافون كاش بتاعك وتشوف آخر التحويلات الواردة.
2. تقارن **المبلغ** + **رقم الموبايل اللي حوّل منه** (والكود المرجعي لو المستخدم كتبه في خانة "ملاحظة التحويل") مع الصفوف اللي حالتها `awaiting_review` في جدول `hour_purchases`.
3. لو التحويل مطابق، تنفّذ من الـ Supabase SQL editor (بصلاحية service role):

   ```sql
   select public.review_manual_hour_purchase(
     '<purchase-id>',
     'paid',
     'تم التأكيد يدويًا'
   );
   ```

   الدالة دي (زي `process_payout_request` تمامًا) بتتأكد إنك بتستخدم `service_role`، وبتضيف الساعات لمحفظة المستخدم أوتوماتيك وبشكل idempotent (لو نفّذتها مرتين على نفس الطلب مش هتضاعف الساعات).
4. لو التحويل مش مطابق أو مزيف، ترفض بنفس الدالة:

   ```sql
   select public.review_manual_hour_purchase('<purchase-id>', 'failed', 'مفيش تحويل مطابق');
   ```

### قبل النشر

- عدّل `lib/payment-methods.ts` وحط رقم Instapay الحقيقي بتاعك ورقم محفظة فودافون كاش الحقيقي.
- نفّذ migration `026_manual_payment_methods.sql` في Supabase.
- ده حل مؤقت وفيه تأخير بشري؛ لما Paymob توافق، ترجّع زرار الدفع الفوري في `components/plans-client.tsx` يستخدم `/api/paymob/create-intention` تاني (الكود لسه موجود ومتربط بالكامل).

## تحديث: لوحة أدمن لمراجعة الطلبات — `/admin/payments`

بدل مراجعة الطلبات من Supabase SQL editor، فيه صفحة داخل الموقع نفسه:

- **الرابط:** `/admin/payments`
- **الحماية:** مش كلمة سر منفصلة — الصفحة بتستخدم نفس تسجيل الدخول العادي بتاع ورّيني (إيميل وباسورد Supabase)، وبعد ما تسجل دخول بيتحقق إن الإيميل بتاعك هو المسموح له بس. الإيميل المسموح دلوقتي: `mt1817221@gmail.com`.
- لو حد سجّل دخول بحساب تاني (أو مش مسجّل خالص)، هيشوف رسالة "مفيش صلاحية دخول" ولا هيقدر يفتح أي بيانات.
- تقدر تضيف إيميلات تانية مسموح لها من غير ما تلمس الكود: حط متغيّر بيئة `ADMIN_EMAILS` بقيمة إيميلات مفصولة بفاصلة، مثلاً:
  `ADMIN_EMAILS=mt1817221@gmail.com,partner@example.com`
- لازم يكون عندك حساب فعلي على ورّيني بنفس الإيميل ده (سجّل عادي من `/signup` لو لسه معملتش)، لأن الصفحة بتتحقق من حساب حقيقي مسجّل دخول، مش كلمة سر منفصلة.
- الصفحة بتعرض كل الطلبات اللي حالتها "قيد المراجعة" مع اسم المستخدم، المبلغ، الساعات، كود الطلب، ورقم الموبايل اللي حوّل منه — وزرار "تأكيد الدفع" أو "رفض" لكل طلب. الضغط على "تأكيد الدفع" بينفّذ RPC `review_manual_hour_purchase` بنفس الأمان اللي شرحناه فوق (idempotent، ومحصور على service role).
- الصفحة متعلّم عليها `noindex` عشان محركات البحث متفهرسهاش.
