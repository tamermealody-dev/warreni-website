# علّمني — ربط Auth الحقيقي (خطوة 2)

> ملاحظة: النسخة اللي وصلتك قبل كده من الحزمة دي كانت ناقصة `components/auth-page.tsx`
> و `.env.local.example` — دول اتضافوا دلوقتي وموجودين في الحزمة الكاملة. باقي الملفات
> (client.ts, server.ts, middleware.ts, proxy.ts, صفحة البروفايل) زي ما هي بالظبط.

## إيه اللي اتغيّر

| الملف | جديد / اتعدّل | ليه |
|---|---|---|
| `lib/supabase/client.ts` | جديد | Supabase client للاستخدام في المتصفح (Client Components) |
| `lib/supabase/server.ts` | جديد | Supabase client للاستخدام في السيرفر (Server Components/Actions) |
| `lib/supabase/middleware.ts` | جديد | بيحدّث الـ session، وبيحمي صفحات profile/messages/transactions |
| `proxy.ts` | جديد (اسمه القديم كان middleware.ts) | Next.js 16 غيّر اسم الاتفاقية دي — لازم يبقى اسمه proxy.ts بالظبط |
| `components/auth-page.tsx` | اتعدّل | التسجيل والدخول بقوا بيكلموا Supabase فعليًا بدل ما كانوا وهميين |
| `app/profile/page.tsx` | اتعدّل (زودنا حاجة بسيطة) | ضفنا زرار "تسجيل الخروج" في الهيدر |
| `.env.local.example` | جديد | نموذج للمتغيرات البيئية المطلوبة |

## خطوات التركيب (بالترتيب)

### 1. ثبّت مكتبة Supabase في مشروعك
جوه مجلد المشروع، من الـ Terminal:
```
pnpm add @supabase/ssr @supabase/supabase-js
```

### 2. انسخ الملفات دي فوق مشروعك
انسخ كل الملفات والمجلدات اللي جوه الحزمة دي بنفس المسارات بالظبط فوق مشروعك
(يعني `lib/supabase/` هتتحط جوه `lib/` بتاعتك، و`proxy.ts` في جذر المشروع... إلخ).
لو عندك ملف `middleware.ts` قديم في جذر مشروعك، **امسحه** — استبدلناه بـ `proxy.ts`.

### 3. اعمل ملف `.env.local`
انسخ `.env.local.example` وسمّيه `.env.local`، واملأ فيه القيمتين الحقيقيتين
اللي أخدتهم من Supabase (Project Settings → API):
```
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
```
`.env.local` أصلاً متجاهل في `.gitignore` فمش هيترفع لأي حد بالغلط.

### 4. قرار: تأكيد الإيميل هيبقى شغال ولا لأ؟
افتراضيًا Supabase بيتطلب من المستخدم يفتح رابط تأكيد في إيميله قبل ما يقدر
يسجل دخول (الكود عندي بيتعامل مع الحالة دي ويوريه شاشة "افتح إيميلك").
لو عايز تسهّل تجربتك وانت بتختبر بس، تقدر توقف التأكيد مؤقتًا من:
**Authentication → Providers → Email → أطفي "Confirm email"**.
لو سبته شغال (المفضّل قبل ما تنشر الموقع للناس)، التدفق هيشتغل عادي بس
هيحتاج المستخدم يفتح إيميله فعلًا.

### 5. شغّل المشروع وجرّب
```
pnpm dev
```
افتح `/signup`، اعمل حساب بإيميل حقيقي تقدر توصله، وشوف صفك بيتسجل في
Supabase (Table Editor → profiles وwallets هتلاقي صف جديد اتعمل تلقائي).
جرّب بعدين تسجل خروج من زرار الخروج في هيدر البروفايل، وسجّل دخول تاني.

## ملاحظة مهمة: Next.js 16 وproxy.ts
لاحظت إن مشروعك شغال بـ Next.js 16، واللي غيّر اسم ملف الـ middleware
التقليدي (`middleware.ts`) لاسم جديد هو `proxy.ts` (نفس الوظيفة بالظبط،
بس الاسم اتغيّر). عشان كده استخدمت `proxy.ts` مباشرة، فمتقلقش لو شفت
الاسم ده غريب أول مرة.

## اللي لسه ما اتوصلش بيه
- صفحة profile لسه فيها بيانات وهمية (الطلبات، المهارات، الرصيد) —
  دي هتتوصل بالداتا الحقيقية في خطوة الـ Explore/Booking API الجاية.
- زرار "المتابعة باستخدام Google" لسه معطّل (طبقًا لقرارنا نأجل الـ social
  login للمراحل الجاية).

## الخطوة الجاية
لما تجرب وتتأكد إن التسجيل/الدخول شغالين، هنبني الـ API الحقيقي لصفحة
Explore (البحث والفلترة من قاعدة البيانات) وربط البروفايل الحقيقي بدل
البيانات الوهمية.
