# الدفع داخل الجلسات — ورّيني

تمت إضافة مسار دفع للجلسة بجانب تبادل الساعات.

## القواعد
- سعر الساعة: 20 جنيه.
- المنصة: 8% عمولة.
- مقدم الخدمة: 92% من قيمة الجلسة.
- الدفع يتم إلى حساب التاجر في Paymob.
- المبلغ يظهر لمقدم الخدمة كمستحق معلّق بعد تأكيد الدفع.
- بعد انتهاء الجلسة بنجاح، ينتقل مستحق مقدم الخدمة من `pending_earnings_egp` إلى `balance_egp`.
- غرفة الجلسة لا تُنشأ أصلًا لجلسة مدفوعة إلا بعد تأكيد Paymob الموقّع بالـ HMAC.
- لو الجلسة أُغلقت بسبب مغادرة أحد الطرفين، العملية تنتقل إلى `refund_pending` بدل صرف مستحق مقدم الخدمة، لتُراجع عملية رد المبلغ.

## قاعدة البيانات
شغّل migration:
`supabase/migrations/020_money_sessions.sql`

## التشغيل
لا تحتاج مفاتيح Paymob جديدة. مسار الجلسات المدفوعة يستخدم نفس:
- `PAYMOB_SECRET_KEY`
- `PAYMOB_PUBLIC_KEY`
- `PAYMOB_HMAC_SECRET`
- `PAYMOB_INTEGRATION_ID_CARD`
- `APP_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

## ملاحظة عن الأرصدة المالية
`balance_egp` هو رصيد مالي مستحق داخل ورّيني، وليس تحويلًا بنكيًا تلقائيًا إلى المستخدم. أموال العميل تدخل حساب التاجر في Paymob، ويمكن للمنصة بعد ذلك تنفيذ تسوية مستحقات مقدمي الخدمات بآلية السحب/التحويل التي ستُضاف لاحقًا.
