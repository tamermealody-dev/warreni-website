# نظام طلبات السحب

بعد تشغيل `021_payout_accounts.sql` شغّل أيضًا:

```sql
-- Supabase SQL Editor
supabase/migrations/022_payout_requests.sql
```

## كيف يعمل

1. المستخدم يضيف بيانات وسيلة السحب من `/payout`.
2. يكتب المبلغ المطلوب سحبه.
3. السيرفر يستدعي `request_payout(amount)` كعملية واحدة داخل قاعدة البيانات.
4. يتم التأكد من وجود وسيلة سحب ومن كفاية الرصيد، ثم حجز المبلغ فورًا بخصمه من `wallets.balance_egp` وإنشاء طلب حالته `pending`.
5. يتم حفظ نسخة من بيانات وسيلة السحب داخل الطلب نفسه، لذلك تعديل بيانات السحب لاحقًا لا يغيّر طلبًا قديمًا.
6. المستخدم يرى حالة كل طلب من نفس الصفحة.
7. عند تنفيذ التحويل فعليًا، النظام الموثوق/الإداري يستطيع استخدام `process_payout_request(request_id, 'paid')`.
8. لو الطلب مرفوض أو ملغي، استدعاء `process_payout_request(request_id, 'rejected')` أو `'cancelled'` يرجّع المبلغ تلقائيًا لرصيد المستخدم.

## مهم

- المستخدم لا يستطيع إدخال أو تعديل `payout_requests` مباشرة عبر RLS؛ الإنشاء يتم فقط عبر الدالة الذرية `request_payout`.
- دالة المعالجة مقصورة على `service_role` ويجب استدعاؤها من backend إداري موثوق، وليس من متصفح المستخدم.
- هذا النظام يسجل طلب السحب ويحجز الرصيد. التحويل البنكي/المحفظة الفعلي يحتاج قناة صرف أو عملية إدارية منفصلة.
