# ورّيني — الـ Explore والبروفايل الحقيقيين (خطوة 3)

## اللي اتعمل في الخطوة دي

| الملف | جديد / اتعدّل | ليه |
|---|---|---|
| `lib/format.ts` | جديد | دوال مشتركة لتنسيق الأرقام العربية، التواريخ، الأحرف الأولى، أسماء الفئات |
| `app/explore/page.tsx` | اتعدّل بالكامل | بقى Server Component بيجيب المستخدمين ومهاراتهم وتقييماتهم فعليًا من Supabase |
| `components/explore-client.tsx` | جديد | واجهة البحث والفلاتر (نفس تصميم الأصل)، بس شغالة على داتا حقيقية |
| `app/profile/page.tsx` | اتعدّل بالكامل | Server Component بيجيب البروفايل، المحفظة، المهارات، الطلبات، كشف الحساب |
| `app/profile/actions.ts` | جديد | Server Actions: قبول/رفض/إلغاء طلب، تأكيد الجلسة أو الإبلاغ عن مشكلة |
| `components/profile-client.tsx` | جديد | واجهة البروفايل (نفس تصميم الأصل)، متصلة بالداتا والأكشنز الحقيقية |

## اللي بقى شغال فعليًا
- صفحة Explore: كل المستخدمين اللي عندهم مهارات مضافة فعليًا (`skills_offered`) بيظهروا،
  مع تقييمهم الحقيقي (متوسط `reviews`) وعلامة "موثّق" الحقيقية (`phone_verified`).
  البحث والفلترة (فئة المهارة / موثّق فقط) شغالين على الداتا دي مباشرة.
- صفحة profile: الاسم، المدينة، تاريخ الانضمام، رصيد الساعات، عدد التبادلات
  الناجحة، متوسط التقييم، المهارات المضافة — كله حقيقي من Supabase.
- الطلبات الواردة/الصادرة (`bookings` بحالة `pending`) بتتعرض فعليًا، وفيها
  أزرار قبول/رفض (للطلبات الواردة) وإلغاء (للطلبات الصادرة) شغالة حقيقي.
- لو عندك جلسة `accepted`، بتظهر كارت "الجلسة" وتقدر تأكد إنها اتمت أو تبلّغ
  عن مشكلة — وده بيسجل في `booking_confirmations` ويشغّل نفس الـ trigger اللي
  بيحوّل الساعات لما الطرفين يأكدوا (002_functions_and_triggers.sql).
- كشف الحساب (درج المحفظة) بيعرض آخر العمليات الحقيقية من `wallet_transactions`.

## اللي لسه مش موجود (الخطوة الجاية)
- **إنشاء طلب تبادل جديد** (زرار "اطلب تبادل" من كارت شخص في Explore) — محتاج
  فورم فيه اختيار المهارة/التاريخ/الساعات وإدخاله في `bookings`، مع مراعاة حد
  الـ -3 ساعات المذكور في `001_schema.sql`.
- **تعديل الملف الشخصي** وإضافة/حذف مهارة — الزراير موجودة بس متعطّلة (disabled) قصدًا.
- **صفحة Messages والمحادثات الحقيقية** — لسه mock بالكامل.
- **صفحة Transactions الكاملة** (الجدول التفصيلي) — لسه mock، بس درج المحفظة
  في البروفايل بقى حقيقي.

## تركيب سريع
نفس خطوات المرة اللي فاتت (pnpm install لو مش عامل، تأكد إن .env.local
موجود ومظبوط، pnpm dev). مفيش متغيرات بيئة جديدة ولا SQL جديد مطلوب تشغله —
كل الجداول والـ triggers مستخدمة زي ما هي.
